---
name: Button
menu: Atoms
route: /button/
---

import { Playground } from 'docz';
import { PrimaryButton, SecondaryButton, TertiaryButton } from '/';

import DoczWrapper from 'documents/DoczWrapper';

# Button

## Basic Usage

<Playground>
  <DoczWrapper>
    <PrimaryButton>
      Primary
    </PrimaryButton>
    <PrimaryButton size='mini'>
      Primary mini
    </PrimaryButton>
    <SecondaryButton>
      Secondary
    </SecondaryButton>
    <SecondaryButton size='mini'>
      Secondary mini
    </SecondaryButton>
    <TertiaryButton>
      Tertiary
    </TertiaryButton>
    <TertiaryButton size='mini'>
      Tertiary mini
    </TertiaryButton>
    <SecondaryButton disabled>
      DISABLED
    </SecondaryButton>
    <TertiaryButton
      size='mini'
      disabled
    >
      DISABLED mini
    </TertiaryButton>
    <PrimaryButton
      onClick={() => alert('Alert')}
    >
      With Click Event
    </PrimaryButton>
    <SecondaryButton
      as='a'
      href='https://piction.network'
      target='_blank'
    >
      Button Style Link
    </SecondaryButton>
  </DoczWrapper>
</Playground>
